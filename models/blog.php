<?php
    // クラスとメソッドを定義
    class Blog {
        // プロパティ

        // コンストラクタ
          // DBファイルの読み込み
          // プロパティにDBファイルの$dbを渡す処理

        function index() {
            echo 'Blogモデルクラスindex()呼び出し';
            // SQL作成 (blogsテーブルデータ全件取得)
            // SQL実行
            // 繰り返し処理で配列を作成
            // 戻り値としてcontrollerへ結果を渡す
        }
    }
